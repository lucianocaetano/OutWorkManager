import { ConfigEnv } from 'vite';
import type { ServeChecker, BuildCheckBin, ServeAndBuildChecker, SharedConfig } from './types';
interface WorkerScriptOptions {
    absFilename: string;
    buildBin: BuildCheckBin;
    serverChecker: ServeChecker;
}
interface Script<T> {
    mainScript: () => (config: T & SharedConfig, env: ConfigEnv) => ServeAndBuildChecker;
    workerScript: () => void;
}
export declare function createScript<T>({ absFilename, buildBin, serverChecker, }: WorkerScriptOptions): Script<T>;
export {};
//# sourceMappingURL=worker.d.ts.map