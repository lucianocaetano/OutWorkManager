"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ensureCall = exports.lspDiagnosticToViteError = exports.range2Location = exports.uriToAbsPath = exports.tsDiagnosticToViteError = exports.formatHost = void 0;
const fs_1 = require("fs");
const os_1 = __importDefault(require("os"));
const strip_ansi_1 = __importDefault(require("strip-ansi"));
const typescript_1 = __importDefault(require("typescript"));
const code_frame_1 = require("@babel/code-frame");
const codeFrame_1 = require("./codeFrame");
/**
 * TypeScript utils
 */
exports.formatHost = {
    getCanonicalFileName: (path) => path,
    getCurrentDirectory: typescript_1.default.sys.getCurrentDirectory,
    getNewLine: () => typescript_1.default.sys.newLine,
};
function tsDiagnosticToViteError(d) {
    var _a, _b, _c, _d, _e, _f;
    const pos = d.start === undefined ? null : (_a = d.file) === null || _a === void 0 ? void 0 : _a.getLineAndCharacterOfPosition(d.start);
    let loc;
    if (pos) {
        loc = {
            file: (_b = d.file) === null || _b === void 0 ? void 0 : _b.fileName,
            line: pos.line + 1,
            column: pos.character + 1,
        };
    }
    // has detail message
    if (d.file && typeof d.start === 'number' && typeof d.length === 'number') {
        return {
            message: strip_ansi_1.default(typescript_1.default.flattenDiagnosticMessageText(d.messageText, exports.formatHost.getNewLine()) +
                os_1.default.EOL +
                os_1.default.EOL +
                codeFrame_1.createFrame({
                    source: d.file.text,
                    location: codeFrame_1.tsLocationToBabelLocation({
                        start: (_c = d.file) === null || _c === void 0 ? void 0 : _c.getLineAndCharacterOfPosition(d.start),
                        end: (_d = d.file) === null || _d === void 0 ? void 0 : _d.getLineAndCharacterOfPosition(d.start + d.length),
                    }),
                })),
            stack: '',
            id: (_e = d.file) === null || _e === void 0 ? void 0 : _e.fileName,
            plugin: 'vite-plugin-checker',
            loc,
        };
    }
    // no detail message
    return {
        message: typescript_1.default.flattenDiagnosticMessageText(d.messageText, exports.formatHost.getNewLine()),
        stack: '',
        id: (_f = d.file) === null || _f === void 0 ? void 0 : _f.fileName,
        plugin: 'vite-plugin-checker',
        loc,
    };
}
exports.tsDiagnosticToViteError = tsDiagnosticToViteError;
/**
 * LSP utils
 */
function uriToAbsPath(uri) {
    return uri.slice('file://'.length);
}
exports.uriToAbsPath = uriToAbsPath;
function range2Location(range) {
    return {
        start: {
            line: range.start.line + 1,
            column: range.start.character + 1,
        },
        end: {
            line: range.end.line + 1,
            column: range.end.character + 1,
        },
    };
}
exports.range2Location = range2Location;
function lspDiagnosticToViteError(diagnostics) {
    if (!diagnostics.diagnostics.length)
        return null;
    const d = diagnostics.diagnostics[0];
    const absPath = uriToAbsPath(diagnostics.uri);
    const range = d.range;
    let loc;
    if (range) {
        loc = {
            file: absPath,
            line: range.start.line + 1,
            column: range.start.character + 1,
        };
    }
    const fileText = fs_1.readFileSync(absPath, 'utf-8');
    const location = range2Location(d.range);
    const columns = code_frame_1.codeFrameColumns(fileText, location);
    // has detail message
    if (d.message && typeof d.range === 'object') {
        return {
            fileText,
            message: strip_ansi_1.default(typescript_1.default.flattenDiagnosticMessageText(d.message, exports.formatHost.getNewLine()) +
                os_1.default.EOL +
                os_1.default.EOL +
                columns),
            stack: '',
            id: absPath,
            plugin: 'vite-plugin-checker',
            loc,
        };
    }
    // no detail message
    return {
        fileText,
        message: typescript_1.default.flattenDiagnosticMessageText(d.message, exports.formatHost.getNewLine()),
        stack: '',
        id: absPath,
        plugin: 'vite-plugin-checker',
        loc,
    };
}
exports.lspDiagnosticToViteError = lspDiagnosticToViteError;
/**
 * Others
 */
function ensureCall(callback) {
    setTimeout(() => {
        callback();
    });
}
exports.ensureCall = ensureCall;
